{
  "name": "cross-chain-payment-gateway",
  "version": "1.0.0",
  "description": "Example 10: Cross-Chain Payment Gateway - Accept payments on any chain, settle to one",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:base": "npx hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:avax": "npx hardhat run scripts/deploy.js --network avalancheFuji",
    "test:payment": "npx hardhat run scripts/testPayment.js --network avalancheFuji",
    "check:status": "npx hardhat run scripts/checkPaymentStatus.js --network avalancheFuji"
  },
  "keywords": ["cross-chain", "payments", "onlyswaps", "settlement"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.28.0",
    "onlyswaps-solidity": "^0.0.9"
  }
}
